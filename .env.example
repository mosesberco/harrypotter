# Google Analytics 4 — the measurement id from the GA property's data stream.
# Set it in Vercel (Project → Settings → Environment Variables) and redeploy.
# Leave it empty and no analytics script is loaded at all.
NEXT_PUBLIC_GA_ID=
